{
  "name": "trevors-data",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "shell": "docker run -it --entrypoint /bin/bash --mount type=bind,source=$(pwd),target=/app trevor:develop",
    "build": "docker build . -t trevor:develop"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "app-store-scraper": "^0.13.0",
    "google-play-scraper": "^6.1.0",
    "json2csv": "^4.2.1",
    "sync-disk-cache": "^1.3.2",
    "sync-request": "^6.0.0"
  }
}
